---
name: video-meme-craft
description: >
  短视频链接一键生成梗图描述词。当用户粘贴抖音、快手、小红书、视频号等短视频分享链接，
  或说"做个梗图""生成梗图""梗图描述词""meme"时触发。自动提取视频文案，提炼核心矛盾/笑点，
  套用 A–G 框架（素材→指令→尺寸→文字→插画→处理→导出），根据视频时长动态决定生成图片数量，
  输出多个纯文本代码块（拆解表 + 描述词 + 规范模板 + 使用流程），可直接喂给 AI 绘图工具。
  即使用户只发了一个链接没有其他说明，也应触发本技能。
---

# Video Meme Craft — 短视频梗图描述词生成器

## 概述

用户粘贴一个短视频链接，本技能自动完成从文案提取到梗图描述词生成的全流程，无需逐步确认。
根据视频内容时长动态决定生成图片数量，风格根据内容自动匹配，不锁定单一风格。

## 触发条件

- 用户消息中包含短视频分享链接（抖音 `v.douyin.com`、快手 `v.kuaishou.com`、小红书 `xhslink.com`、视频号等）
- 用户说"做个梗图""生成梗图""梗图描述词""meme"等
- 用户发链接 + 简单说明（如"帮我做个梗图"）

## 执行流程

### Step 1: 提取视频文案与时长

调用 `douyin-copy-extract` 技能（或等效的链接提取能力），拉取：
- 视频标题
- 视频简介/描述
- 口播文案（如有）
- 视频时长（秒）

将提取结果作为 **A. 原始素材**。

### Step 2: 计算图片生成数量

根据视频时长动态决定生成图片数量：

| 视频时长 | 建议图片数 | 说明 |
|----------|-----------|------|
| ≤ 15 秒 | 2~3 张 | 短视频内容精简，抓核心即可 |
| 16~30 秒 | 4~6 张 | 有一定叙事，可分段描述 |
| 31~60 秒 | 7~10 张 | 内容较丰富，按段落拆分 |
| 1~3 分钟 | 10~20 张 | 长视频按内容节点拆分 |
| > 3 分钟 | 20~30 张 | 深度内容，按主题段落拆分 |

- 基准规则：约每 6~8 秒内容生成 1 张图片
- 最终数量根据内容密度适当调整（信息密集多生成，节奏舒缓少生成）
- 用户可在链接后指定数量覆盖（如"只要 5 张"）

### Step 3: 提炼核心矛盾点

从文案中识别：
- 核心冲突或矛盾（如"越没人买越涨价"）
- 笑点/槽点/反差
- 情绪基调（愤怒/无奈/嘲讽/自嘲）

提炼为一句 ≤30 字的素材定稿。
若生成多张图片，为每张图片提炼不同的矛盾点/笑点。

### Step 4: 套用 A–G 框架

按以下框架自动填充各点内容。默认参数见下方"默认参数"表，用户可在链接后附带说明覆盖。

| 点 | 角色 | 填充规则 |
|---|------|---------|
| **A** | 原始素材 | Step 3 提炼的矛盾点定稿 |
| **B** | 生成指令 | 固定文案："按照以下 C–G 要求生成图片" |
| **C** | 尺寸约束 | 默认 1920×1080（16:9 横版，Full HD） |
| **D** | 文字内容 | 主标题 ≤12 字（抓眼球）+ 副文案 ≤25 字（补刀/反问） |
| **E** | 插画元素 | 2~4 个核心视觉元素，每个描述角色/动作/道具/表情 |
| **F** | 视觉处理 | 保证文字清晰可读，处理方式根据内容和风格灵活选择 |
| **G** | 导出规格 | 默认 2× 高清（3840×2160），PNG 格式 |

### Step 5: 确定风格

风格不固定，根据视频内容自动匹配：

| 内容类型 | 推荐风格 | 说明 |
|----------|---------|------|
| 科技/硬件吐槽 | 扁平矢量插画风 | 线条简洁，色块明快 |
| 生活/情感类 | 手绘水彩风 | 笔触柔和，色调温暖 |
| 职场/社会现象 | 美式漫画风 | 粗线条，夸张表情 |
| 搞笑/反转类 | Q版卡通风 | 圆润造型，表情夸张 |
| 知识/科普类 | 信息图风格 | 图标+数据可视化 |
| 愤怒/吐槽类 | 波普艺术风 | 高对比度，冲击力强 |

- 以上为参考，实际风格根据内容自由选择
- 用户可在链接后指定风格覆盖（如"用水彩风"）
- 同一组图片可混合不同风格，根据每张图的内容匹配

### Step 6: 输出纯文本代码块

依次输出以下 4 个代码块，每个用 ``` 包裹，方便用户一键复制。
若生成多张图片，代码块 1 和 2 按图片编号依次输出。

#### 代码块 1: 各点内容拆解表

```
A|原始素材|<提炼后的矛盾点>
B|生成指令|按照以下 C–G 要求生成图片
C|尺寸约束|<宽>×<高>（<比例>，<用途>）
D|文字内容|主标题「<标题>」+ 副文案「<副文案>」
E|插画元素|<元素1>、<元素2>、<元素3>
F|视觉处理|<处理描述>
G|导出规格|<路径>，<倍率>高清（<实际像素>）
```

#### 代码块 2: 图片生成描述词

将 A–G 各点展开为完整的 AI 绘图提示词，包含：
- 画面尺寸
- 整体风格（根据内容自动匹配，不锁定单一风格）
- 构图布局（根据风格和内容灵活安排）
- 主标题/副文案排版（字体、字号、颜色、位置）
- 逐个插画元素详细描述
- 配色方案（主色/辅色/警示色/背景色）
- 导出参数

#### 代码块 3: 梗图生成规范 v1.1

A–G 七点标准化模板（可复用），格式同代码块 1 但用通用占位符。

#### 代码块 4: 使用流程

```
1. 填入 A（素材）      → 提炼矛盾/笑点
2. 确认 C（尺寸）      → 根据发布平台选择
3. 撰写 D（文字）      → 主标题抓眼球，副文案补刀
4. 设计 E（插画）      → 元素不超过 4 个，避免杂乱
5. 设定 F（处理）      → 保证文字清晰可读
6. 指定 G（导出）      → 2× 高清是公众号/小红书最低要求
7. 拼合完整描述词      → 喂给 AI 绘图工具生成
```

## 默认参数

| 参数 | 默认值 | 覆盖方式 |
|------|--------|---------|
| 尺寸 | 1920×1080（16:9） | 链接后附带说明 |
| 导出倍率 | 2× 高清（3840×2160） | 同上 |
| 风格 | 根据内容自动匹配 | 同上 |
| 图片数量 | 根据视频时长动态计算 | 同上 |
| 元素数量 | 2~4 个 | 同上 |
| 格式 | PNG | 同上 |

## 参数覆盖示例

```
https://v.douyin.com/xxxxx/ 帮我做个正方形的，1倍导出
→ C 改为 1080×1080（1:1），G 改为 1× 高清

https://v.douyin.com/xxxxx/ 竖版发朋友圈
→ C 改为 1080×1920（9:16）

https://v.douyin.com/xxxxx/ 用水彩风，只要5张
→ 风格改为手绘水彩风，图片数量固定为 5 张
```

## 依赖

- `douyin-copy-extract` 技能（用于短视频链接文案提取）
- 如该技能不可用，降级为：提示用户手动粘贴视频文案，继续执行 Step 2–6

## 设计原则

- **自主跑完**：不需要逐步确认，一次输出全部代码块
- **纯文本优先**：所有输出用代码块包裹，方便一键复制
- **矛盾驱动**：梗图的核心是矛盾/反差，Step 3 是关键
- **元素克制**：插画元素不超过 4 个，避免画面杂乱
- **风格自由**：不锁定单一风格，根据内容自动匹配最佳风格
- **数量自适应**：图片数量根据视频时长动态计算，内容越长图片越多

## 参考文件

- `references/framework.md` — A–G 框架的详细定义与历史迭代记录
- `assets/template.md` — 输出模板（代码块 3 的完整版本）